# Where `astro build` fetches the PUBLISHED admin-managed landing copy
# (Convex HTTP route — note .convex.site, not .convex.cloud).
# Unset = the build uses the checked-in dicts from @workspace/landing-content.
LANDING_CONTENT_URL=https://<deployment>.convex.site/landing-content
