// The content shape moved to @workspace/landing-content (shared with the
// Convex backend and the admin editor). This re-export keeps the components'
// existing import paths working.
export type {
  Dict,
  Lang,
  Stat,
  Step,
  IconStep,
  Tier,
  Testimonial,
  QA,
  FooterCol,
} from "@workspace/landing-content"
