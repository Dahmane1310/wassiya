---
import LandingPage from "../components/LandingPage.astro"
import { loadLanding } from "../i18n/load"

const { t, images } = await loadLanding("en")
---

<LandingPage t={t} lang="en" images={images} />
